class Solution(object):
    def multiply(self, num1, num2):
        """
        :type num1: str
        :type num2: str
        :rtype: str
        """
        # we can create here two linkedlist and then perform the multiply operation and return a string
        return str(int(num1)*int(num2))
